
                                    //string to int array
                                    //int[] numbers = new int[msg.ToString().Length];
                                    //for (int i = 0; i < numbers.Length; i++)
                                    //{
                                    //    //string to number array
                                    //    numbers[i] = Convert.ToInt32(msg[i]);
                                    //}
                                    //String dataReceived = intToHex(numbers);
